import { createTheme } from "@material-ui/core/styles";

const navigationTheme = createTheme({
  typography: {
    fontFamily: ["Tangerine", "Open Sans"].join(","),
  },
});

export {navigationTheme}


